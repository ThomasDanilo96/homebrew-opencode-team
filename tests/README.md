# Tests

Portable package tests will be added as runtime configuration is parameterized.

The staging tests are intentionally not copied because they reference local
sandboxes, runtime databases, sessions, and machine-specific paths.
