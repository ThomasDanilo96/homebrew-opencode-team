# DAILY Benchmark Fixture

This fixture is copied into a private task worktree for each prepared run. It is
never the user's repository and contains no credentials or production state.

The task manifest supplies the objective and deterministic verification contract;
the fixture is reset before every profile execution.
